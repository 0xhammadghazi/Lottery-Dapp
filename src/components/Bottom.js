const Bottom = () => {
  return (
    <h4 className="bottom">
      If Chainlink's Alarm Clock Node of Kovan Network is down, then the Lottery
      Dapp will not work as intended!
    </h4>
  );
};

export default Bottom;
